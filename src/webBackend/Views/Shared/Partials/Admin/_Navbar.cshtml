<nav class="navbar bg-dark border-bottom border-body navbar-expand-lg" data-bs-theme="dark">
    <div class="container">
        <a asp-controller="Admin" asp-action="Index" class="navbar-brand">Admin</a>

        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarContent">
            <span class="navbar-toggler-icon"></span>
        </button>

        <div class="collapse navbar-collapse" id="navbarContent">
            <ul class="navbar-nav me-auto mb-2 mb-lg-0">
                
                @if (User.IsInRole("Admin") || User.HasClaim("Permission", "Category.View"))
                {
                    <li class="nav-item">
                        <a asp-controller="Kategori" asp-action="Index" class="nav-link">Kategori</a>
                    </li>
                }

                @if (User.IsInRole("Admin") || User.HasClaim("Permission", "Product.View"))
                {
                    <li class="nav-item">
                        <a asp-controller="Urun" asp-action="Index" class="nav-link">Ürünler</a>
                    </li>
                }

                @if (User.IsInRole("Admin") || User.HasClaim("Permission", "Order.View"))
                {
                    <li class="nav-item">
                        <a asp-controller="Order" asp-action="Index" class="nav-link">Sipariş</a>
                    </li>
                }

                @if (User.IsInRole("Admin"))
                {
                    <li class="nav-item dropdown">
                        <a class="nav-link dropdown-toggle" href="#" role="button" data-bs-toggle="dropdown">
                            Yönetim
                        </a>
                        <ul class="dropdown-menu">
                            <li><a asp-controller="Role" asp-action="Index" class="dropdown-item">Rol ve Yetkiler</a></li>
                            <li><a asp-controller="User" asp-action="Index" class="dropdown-item">Kullanıcılar</a></li>
                            <li><a asp-controller="Slider" asp-action="Index" class="dropdown-item">Slider Yönetimi</a></li>
                        </ul>
                    </li>
                }

                @if (User.IsInRole("Admin"))
                {
                    <li class="nav-item dropdown">
                        <a class="nav-link dropdown-toggle text-warning" href="#" role="button" data-bs-toggle="dropdown">
                            Kargo
                        </a>
                        <ul class="dropdown-menu">
                            <li><a class="dropdown-item" asp-controller="Carrier" asp-action="Index">Firmalar</a></li>
                            <li><a class="dropdown-item" asp-controller="ShippingRegion" asp-action="Index">Bölgeler</a></li>
                            <li><a class="dropdown-item" asp-controller="Lojistik" asp-action="KargoIstisnalari">Kısıtlamalar</a></li>
                        </ul>
                    </li>
                }
            </ul>

            <ul class="navbar-nav ms-auto mb-2 mb-lg-0">
                <li class="nav-item">
                    <a href="#" class="nav-link text-info"><i class="fas fa-user me-1"></i>@User.Identity?.Name</a>
                </li>
                <li class="nav-item">
                    <a asp-controller="Account" asp-action="LogOut" class="nav-link text-danger">Çıkış</a>
                </li>
            </ul>
        </div>
    </div>
</nav>